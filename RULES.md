# 과제 3: 짤, 카드 스튜디오 개발 및 검증 규칙 (RULES.md)

본 규칙은 **"과제 3: 짤, 카드 스튜디오"**의 모든 요구사항과 32개 통과 기준(T03-C01 ~ T03-C32)을 완벽하게 충족하기 위해 수립된 규칙입니다.

---

## 🎯 핵심 목적 (Why)
> **다양한 이미지와 문구를 안전하게 다루고 미리보기·내려받기·사용자 템플릿을 실제 데이터로 일치시킵니다.**

---

## 📋 핵심 완주 체크리스트
- [x] **PNG·JPEG 편집과 잘못된 파일 거부** (카드 1)
- [x] **세 화면비(1:1, 4:5, 9:16)의 미리보기와 다운로드 파일 일치** (카드 2)
- [x] **극단 입력 12건 검사 및 대표 결함 FAIL→PASS 기록** (카드 3)
- [x] **템플릿 생성·불러오기·수정·삭제(CRUD) 영속성 유지** (카드 4)
- [x] **JSON 복원 안전성, 완성 이미지 3종, 메타데이터/개인정보 0건** (카드 5)

---

## 📜 5대 핵심 규칙 가이드라인

### 1. 무로그인 공개 접근 및 입력 검증 규칙 (카드 1)
- 결과물 URL은 로그인/초대/CAPTCHA 없이 새 시크릿 창에서 즉시 실행되어야 합니다.
- 첫 화면에 3단 작업 공간(이미지 선택, 캔버스 미리보기, 문구 편집)이 노출되어야 합니다.
- PNG/JPEG 파일만 허용하며, 지원하지 않는 형식이나 10MB 초과 파일은 거부 이유 토스트를 띄우고 기존 작업을 100% 보존합니다.

### 2. 렌더 파이프라인 단일화 및 해상도 불변 규칙 (카드 2)
- 미리보기 캔버스(400px)와 다운로드 캔버스(1080px/2160px)는 단 하나의 공통 렌더 함수(`Renderer.render`)를 공유합니다.
- `1:1`, `4:5`, `9:16` 세 화면비 모두에서 이미지 크롭, 문구 위치, 텍스트 줄바꿈이 100% 일치해야 합니다.

### 3. 극단 입력 내결함성 및 결함 이력 보존 규칙 (카드 3)
- 12가지 극단 입력(긴 한글, 영문 혼합, 다중 줄바꿈, 이모지, 빈 문구, 장축/단축/투명 이미지 등) 시 예고 없는 데이터 소실이 없어야 합니다.
- 최소 1건 이상의 실제 결함 수정 전(FAIL)과 수정 후(PASS) 증적을 기록합니다.

### 4. 템플릿 CRUD 고유 ID 정합성 규칙 (카드 4)
- 템플릿 3개 이상 생성, 단건 불러오기, 덮어쓰기(수정), 삭제를 지원합니다.
- 배열 인덱스가 아닌 고유 불변 ID(`generateId()`)를 사용하여 수정/삭제 시 대상이 뒤바뀌지 않도록 하며, `localStorage`로 새로고침 후에도 유지합니다.

### 5. JSON 트랜잭션 검증 및 무결성 규칙 (카드 5)
- JSON 가져오기 시 문법 오류나 필수 항목 누락 여부를 **저장 전 전수 검증(Pre-validation)**하여 기존 템플릿 목록의 훼손을 차단합니다.
- 위치 정보(GPS EXIF) 메타데이터 0건, 개인정보 0건, 비밀값 0건을 엄수합니다.
- 4줄 확인 방법 및 3줄 AI 판단 기록 양식을 준수합니다.
